import { HashTypes } from '.';
export interface Opts {
    iv: string;
    iterations: number;
    keylet: number;
}
export interface Response {
    bytes: string;
    hex: string;
}
export declare class Index {
    type: HashTypes;
    opts: Opts;
    make: (opts: {
        key: string;
        size?: number;
    }) => Promise<Response>;
}
