import { BaseFullResponse, BaseRequest, BaseResponse, CryptOpts } from './common/base';
import { Extension } from '../../extension/index';
export interface DecryptRequest extends BaseRequest {
    command: Extension.COMMANDS.DECRYPT;
    data: {
        address: string;
        hex: string;
        opts?: CryptOpts;
    };
}
interface DecryptDataResponse {
    data: {
        data: string;
        bytes: string;
        hex: string;
    };
}
export type DecryptResponse = BaseResponse & DecryptDataResponse;
export interface DecryptFullResponse extends BaseFullResponse {
    request: DecryptRequest;
    response: DecryptResponse;
}
export {};
