import { BaseFullResponse, BaseRequest, BaseResponse, CryptOpts } from './common/base';
import { Extension } from '../../extension/index';
export interface EncryptRequest extends BaseRequest {
    command: Extension.COMMANDS.ENCRYPT;
    data: {
        address: string;
        data: string;
        opts?: CryptOpts;
    };
}
interface EncryptDataResponse {
    data: {
        hex: string;
        bytes: string;
        iv: string;
        tag?: string;
    };
}
export type EncryptResponse = BaseResponse & EncryptDataResponse;
export interface EncryptFullResponse extends BaseFullResponse {
    request: EncryptRequest;
    response: EncryptResponse;
}
export {};
