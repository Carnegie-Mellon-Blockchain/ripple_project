import { latest } from '../../schema/index';
declare class Config {
    static config: Config.config;
}
declare namespace Config {
    interface config {
        title: typeof latest.Types.Apps.Apps.AppTypes;
        run: 'development' | 'production' | 'test';
        env: 'chrome' | 'firefox' | 'opera';
        version: string;
        init: {
            background: boolean;
            content: boolean;
        };
        dev: boolean;
        beta: boolean;
        logger: boolean;
        key: string;
        api: {
            endpoint: string;
            secret: string;
        };
    }
}
export { Config };
